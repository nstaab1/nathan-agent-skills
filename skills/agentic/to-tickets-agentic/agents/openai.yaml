interface:
  display_name: "To Tickets Agentic"
  short_description: "Plan AFK and attended implementation lanes"
  default_prompt: "Use $to-tickets-agentic to split this plan into agent-ready and attended tickets."
policy:
  allow_implicit_invocation: false
